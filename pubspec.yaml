name: flutter_std_utils
description: Utils wich duplicates in more projects.
version: 0.0.3
homepage: https://github.com/unger1984/flutter_std_utils

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.4
  flutter_secure_storage: ^5.0.2
  l: ^3.1.0
  shared_preferences: ^2.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  dart_code_metrics: ^4.9.1
  flutter_lints: ^1.0.4
